@model VMS.ViewModel.VMCodeMaster
@using VMS.Helper
@using VMS.Models
@using VMS.ViewModel
@using Microsoft.AspNetCore.Http
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@* @{
    var userAccess = (VMUserRoleAccess)HttpContextAccessor.HttpContext.Session.GetObjectFromJson<VMUserRoleAccess>("userAccess");
}
@if (userAccess.AddUserRoleFullAccess == true || userAccess.AddUserRoleViewAccess == true)
{ *@
<div class="content-wrapper">
    <div class="row">
        <div class="col-12 grid-margin">
            <div class="card">
                <div class="card-body" style="min-height:500px;">
                    <h4 class="card-title">Code Master Details</h4>
                    <div>
                        <form id="frmUserRole" name="frmUserRole" method="post" class="forms-sample" autocomplete="off">
                            @Html.AntiForgeryToken()
                            <div class="row rowHeight">
                                <div class="col-md-6">
                                    <div class="form-group row">
                                        <label class="col-sm-4 col-form-label" for="txtCodeType">Code Type: </label>
                                        <div class="col-sm-8">
                                            @Html.TextBoxFor(m => m.CodeType, new { @class = "form-control", id = "txtCodeType" })
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group row">
                                        <label class="col-sm-4 col-form-label" for="txtCode">Code: </label>
                                        <div class="col-sm-8">
                                            @Html.TextBoxFor(m => m.Code, new { @class = "form-control", id = "txtCode" })
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="row rowHeight">
                                <div class="col-md-6">
                                    <div class="form-group row">
                                        <label class="col-sm-4 col-form-label" for="txtDescription">Description: </label>
                                        <div class="col-sm-8">
                                            @Html.TextBoxFor(m => m.Description, new { @class = "form-control", id = "txtDescription" })
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group row">
                                        <label class="col-sm-4 col-form-label" for="chkIsActive">Is Active:</label>
                                        <div class="col-sm-8">
                                            @Html.CheckBoxFor(x => x.IsActive, new { @class = "form-check-label checkboxStyle", id = "chkIsActive" })
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                    <div class="row rowHeight">
                        <div class="col-md-12"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@* } *@
@section scripts {
    <script type="text/javascript">
        // var fullAccess = '@ViewBag.AddUserRoleFullAccess';
        // var viewAccess = '@ViewBag.AddUserRoleViewAccess';
        var fullAccess = 'True';
        var viewAccess = 'True';

        $(document).ready(function () {
            setSideMenu("make-master");
            if (fullAccess === "True" || viewAccess === "True") {
                makeFieldsReadOnly();
                var makeMasterID = GetParameterValues('makeMasterID');
                getmakeMasterID(makeMasterID);
            }
        });

        function makeFieldsReadOnly() {
            $("#txtCodeType").prop("readonly", true);
            $("#txtCode").prop("readonly", true);
            $("#txtDescription").prop("readonly", true);
            $("#chkIsActive").attr("disabled", true);
        }

        function getmakeMasterID(makeMasterID) {
            $.ajax({
                cache: false,
                url: '@(utilityHelper.SiteUrl() + "MakeMaster/getMakeMasterByID")',
                type: "GET",
                data: { makeMasterID: makeMasterID },
                dataType: "json",
                success: function (data) {
                    console.log(data);
                    if (data != null && data != undefined) {
                        $('#txtCodeType').val(data.codeType);
                        $('#txtCode').val(data.code);
                        $('#txtDescription').val(data.description);
                        $("#chkIsActive").prop('checked', data.isActive);
                    } else {
                        $.confirm({
                            title: 'Code Master',
                            content: 'Code Master does not Exists!',
                            icon: 'fa fa-warning',
                            type: 'red',
                            buttons: {
                                omg: {
                                    text: 'OK',
                                    btnClass: 'btn-red',
                                },
                            }
                        });
                    }
                },
                error: function (ex) {
                    alert(ex);
                }
            });
        }
    </script>
}